{
  "name": "openschematicmaps-site",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone site for the schematic transit map project",
  "scripts": {
    "dev": "eleventy --serve --input=src --output=dist",
    "build": "eleventy --input=src --output=dist"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "playwright": "^1.62.1"
  }
}
